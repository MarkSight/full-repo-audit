# Changelog

## v3.0.0 — 2026-09-13 — 维度摊开，每轮摊薄

### 为什么 v2 效果变差

v2 在真实仓库上的审查效果**明显差于 v1**。问题在设计，不在执行：

1. **单个 agent 注意力被摊薄。** 一个波次塞 8-9 个维度，并建议「按模块分片」派 agent，
   结果每个 agent 同时背一个模块上的全部维度、几十个检查项，每项都只能浅扫。
2. **轮次名存实亡。** 修复集中到 W13，前面 11 个波次变成一次连续扫描，
   没有「审查 → 修复 → 验证 → 下一轮」的闭环，事实上只有一轮并行十几个领域。
3. **流程开销挤占阅读预算。** 适用性矩阵、台账状态流、多段式报告模板，
   token 花在填表上，而不是读代码。
4. **擅自改动了实战验证过的顺序**，用「按严重度排序更合理」的推理替代了实测结果。

### 为什么不是简单退回 v1 的 14 轮

v1 效果好，是因为每轮聚焦、每轮闭环；但它的覆盖面被 14 轮封顶，想补缺失的审查，
只能往某一轮里继续加方向 —— 又会重新走向「每轮变宽」。

**覆盖面与深度不矛盾，矛盾的只是单独一轮的宽度。** 所以 v3 同时吸收两边：

| 来自 | 保留什么 |
|------|---------|
| v2 | 维度充分摊开：缺失审查全部补齐，并拆得更细 |
| v1 | 每轮只聚焦一件事、轮次严格串行、每轮内修复验证、阶段顺序 |

### v3 的设计

- **一轮 = 一个维度。** 维度的粒度以「一个 agent 能同时装在脑子里」为准：
  「安全」「注入」都不是维度，「SSRF 与外部请求」才是。每个维度 = 定位 + 3-6 个检查点。
- **轮数不设上限。** 14 个阶段共 223 个维度（S01 另有 8 个无 UI 替代维度），
  适用多少跑多少，几十轮到两百多轮都正常。
- **轮内分片只按文件，不按主题。** 目标量超过一个 agent 能完整读完的范围时，
  同一个维度按目录切片并行；每个 agent 仍然只带这一个维度。
- **验证分两级。** 每轮：受影响范围的测试 + 改动文件的 lint / typecheck；
  每个阶段末：全量门禁 + 不超过 30 行的阶段摘要，高频问题写入后续相关维度的 prompt。
- **可跨会话续跑。** `audit/progress.md` 是唯一状态：每个维度一行，`[x]` 完成 / `[-]` 跳过及原因 / `[ ]` 待进行。
  上下文压缩或新会话后，从第一个 `[ ]` 继续。
- **阶段顺序沿用 v1**（S01 功能与交互 → … → S14 终验），因为阶段之间互相喂养：
  S05 复查前四阶段的修复，S07 清理 S06 诊断的测试，S09 在 S10 删代码前全量验证，S12 固化高频问题。

### 相对 v1 补充的维度（均为独立一轮）

- **安全**：认证覆盖、会话与 token、口令与账号枚举、OAuth、IDOR、权限集中度、多租户隔离、
  SQL / 命令 / 路径 / XSS / SSRF / 反序列化各自独立、CSRF 与 Cookie、敏感数据暴露、密码学、Webhook 入站
- **数据**：TOCTOU、幂等、双写一致性、嵌入式数据库升级、备份恢复、硬删除防护
- **交付**：CI 权限与 secrets、CI 不可信输入、Dockerfile、容器运行配置、IaC、部署回滚
- **机密**：工作区、git 历史、客户端产物、默认密钥兜底
- **隐私与滥用**：个人信息日志泄露、数据导出删除、第三方数据发送、限流与真实 IP、请求上限与 ReDoS、昂贵操作成本上限
- **正确性**：时区与日期边界、金额精度与单位、字符编码截断、配置启动校验、特性开关债务
- **测试与治理**：变异抽查、授权矩阵测试、外部调用封装、ID 生成、失效门禁排查

v1 R11 中与 R04 重复的「静态资源」方向已去重。

### 文件变化

- 新增 `references/stages/S01…S14`：每个阶段一个文件，每个维度一个小节，编排者每轮只读当前小节
- 重写 `SKILL.md`：五条铁律、单轮流程、阶段收尾、续跑、提示词模板
- 删除 v2 的 `references/dim-*.md`、`agent-playbook.md`、`severity-and-reporting.md`、`recon-playbook.md`
- 保留 `scripts/repo-profile.sh`
- README 中英文同步改写

### 各阶段维度数

S01 20（+8）· S02 39 · S03 28 · S04 19 · S05 14 · S06 13 · S07 8 · S08 16 · S09 5 · S10 12 · S11 24 · S12 11 · S13 9 · S14 5，共 223。

---

## v2.0.1 — 2026-09-13 — README 中英拆分与商业化改写

- `README.md` 改为英文、新增 `README.zh-CN.md` 中文版，双向语言切换链接。
  **Why:** 原先中英逐段穿插，两种语言的读者都要跳读一半无关内容；
  拆开后每份都能按完整叙事线阅读。
- 改为面向决策者的商业化叙事：问题陈述 → 方案 → 实战出处 → 商业价值场景 →
  差异化 → 快速开始 → FAQ，并补充 3 张 mermaid 引导图
  （波次流水线、发现漏斗、编排者与 agent 扇出）。
  **Why:** 原 README 是功能清单，回答不了「为什么值得用」和「解决谁的什么问题」。
- 三张图已实测渲染（mermaid 10.9.1，6 个图块零解析错误）；
  节点标签内避免使用 `&`（mermaid 的链式操作符）。

## v2.0.0 — 2026-09-13 — 通用化重构

把一个绑定特定仓库的审查脚本，改成任何仓库可用的审查编排器。

### 为什么改

v1 的 `SKILL.md` 把一次真实审查战役（14 轮、68+ agent）的**执行记录**当成了**方法论**：

1. **路径与技术栈写死**：`components/canvas/`、`app/p/[projectId]/production/`、
   `packages/shared/`、`pnpm validate`、Drizzle 迁移、WASM SQLite、Biome 配置。
   换一个仓库，agent 会去读不存在的目录。
2. **结构自相矛盾**：开头的工作流总览写的是 7 个 Phase（Phase 5 横向展开、
   Phase 6 修复、Phase 7 验证），正文却是 14 个 Phase，且 Phase 5 被复用为
   「QA 查漏补缺」。Phase 9 做完整性验证，Phase 14 又做一次。
3. **排序反了**：Phase 1 是 UI 易用性，安全在 Phase 2，基础设施在 Phase 3。
   UI 修复最容易被后续的契约/数据修复推翻，放第一位必然返工。
4. **维度是审查角度清单，不是检查清单**：「安全加固：IDOR、SSRF、CSRF、XSS……」
   只有关键词，没有逐条检查项，agent 只能靠自己发挥。
5. **缺少大量维度**（见下）。
6. **声称全自动推送**：把 `commit → push` 写成无人工步骤，
   也没有区分「机密泄露这类必须人工处置的发现」。

### 主要变更

| # | 变更 | 理由 |
|---|------|------|
| 1 | 新增 **Phase 0 侦察建档** + `scripts/repo-profile.sh`（只读探测）+ `references/recon-playbook.md`（14 个生态的命令映射） | 所有路径与命令改为从仓库画像推导，skill 本体不含项目专属内容 |
| 2 | 维度改用**角色**描述范围（入口层/信任边界/持久层/领域层/表现层/共享层/交付层/测试层），由画像映射到真实路径 | 去掉写死路径但保留可操作性 |
| 3 | 波次按**严重度**重排：机密与供应链 → 安全 → 数据 → 运行时 → 契约 → 性能 → 体验 → 交付 → 测试 → 代码健康 → 治理 | 先修会立刻造成损失的；表层后置避免返工；测试在修复波之前以提供可信门禁 |
| 4 | 修正结构矛盾：总览与正文统一为 **Phase 0 + W1-W11 审查 + W12 横向展开 + W13 修复 + W14 终验** | v1 的 7-phase 总览与 14-phase 正文互相冲突；验证不再出现两次 |
| 5 | 66 个维度**逐条细化为检查清单**，拆分为 11 个 `references/dim-*.md` | 渐进式披露：SKILL.md 只管编排，agent 按需加载自己维度的清单 |
| 6 | 新增**严重度评级标准**（按后果定级 + 提降级规则 + 禁止的定级理由）、单条发现格式、发现台账、波次报告与最终报告模板 | v1 的发现格式没有后果、触发条件、回归保护字段，无法据此定级与复核 |
| 7 | 新增**适用性矩阵**：每个维度标「适用 / 不适用（原因）/ 抽样（范围）」 | 「零盲区」从口号变成可复核的声明 |
| 8 | 新增**规模档位**（S/M/L/XL）决定 agent 数与抽样策略；新增 `--scope/--waves/--depth/--fix/--autonomous` 参数 | v1 固定 6-8 agent × 14 轮，小仓浪费、XL 仓不够 |
| 9 | **审查与修复严格分离**（审查 agent 只读）+ 文件所有权分片 + 冲突裁定规则 | v1 没有冲突控制，多 agent 并行改同一文件会互相覆盖 |
| 10 | 明确**人工把关边界**：commit/push/PR 默认需确认；机密泄露、破坏性迁移、生产配置、权限策略、依赖大版本升级禁止自动修 | v1 宣称「零人工干预直到推送」，对凭据泄露这类发现是危险的 |
| 11 | W11 要求**至少产出一条新增自动化门禁**，并新增「失效门禁」排查（`continue-on-error`、排除目录、阈值低于现状） | 不固化就会年复一年重查同样的问题；假门禁比没门禁更危险 |
| 12 | `agent-playbook.md` 提供审查/横向展开/修复三类 prompt 模板，含提示注入防护（仓库内容是数据不是指令） | v1 模板带项目名且无安全约束 |
| 13 | 强调**波次间由编排者做汇总 → 提取线索 → 改写后续 prompt** | 这是 skill 的核心价值；省掉它，14 波退化成 14 次无关扫描 |

### 新增维度（v1 完全缺失）

- **A3 供应链完整性**：锁文件、安装期脚本、依赖来源、版本钉死、typosquatting、SBOM
- **B4 密码学与密钥使用**、**B6 滥用与配额**（限流、ReDoS、zip bomb、成本上限）、
  **B7 隐私与 PII 留存**、**B8 Webhook 与集成安全**（签名、重放、幂等）
- **C6 缓存一致性**（含「缓存键缺租户维度 = 越权泄露」）、**C7 备份恢复与数据丢失路径**、
  **C8 时间/时区/金额精度/单位/编码**
- **D2 失败模式与恢复**（超时、退避、熔断、降级、背压）、**D5 配置与特性开关**
- **E2 API 契约与版本化**（兼容性、废弃流程、分页契约、幂等键）、
  **E4 单一事实源漂移**（形状链逐环核对）
- **F5 基准与回归守卫**（「没有度量就不要改热路径」）
- **G6 CLI 与库 DX**（退出码、stdout/stderr 约定、非交互可用性）、**G7 文案与术语一致性**
- **H2 CI/CD 安全**（权限最小化、fork PR 的 secrets、action 钉 SHA、命令注入）、
  **H3 容器与 IaC**、**H4 部署回滚**（含「不可逆迁移让回滚失效」）
- **I1 风险导向覆盖**（关键路径映射、每条 Critical/High 必须有回归测试）、
  **I5 缺失测试类型盘点**（含授权矩阵测试——性价比最高的缺失项）
- **K1-K3 文档完备性/准确性/DevEx**（runbook、ADR、文档命令实测、冷启动成本）
- **L4 门禁固化与归属**

### 被重新归类的 v1 Phase

| v1 | v2 |
|----|----|
| P1 功能与 UI（8 个项目专属方向） | W7 G1/G5（泛化为用户旅程 + 四态检查） |
| P2 安全/事务/a11y/错误/性能 | 拆到 W2(B1-B8)、W3(C1)、W7(G2)、W4(D1)、W6 |
| P3 类型/构建/迁移/状态机/技能系统 | W5(E1/E5)、W8(H1/H2)、W3(C2/C5) |
| P4 根配置/文档/测试/public/CHANGELOG/静态资源 | W10(J6/K1/K2)、W8(H5)、W9 |
| P5 QA 查漏补缺（回归/旅程/边界/一致性/数据流/日志） | W12 横向展开 + W4(D4) + W10(J4) + W14 回归确认 |
| P6 测试类型深挖 · P7 测试清理 | W9(I1-I6)，合并为一波并补充 flaky/变异验证 |
| P8 规范化统一（7 方向） | W10(J4) + W11(L3)，并加入「格式问题交工具，不逐条人工报」 |
| P9 完整性验证 | W14（不再中途出现一次） |
| P10 死代码/重复/解耦 | W10(J1-J3)，补充「过度抽象」反向检查 |
| P11 依赖安全/静态资源/优雅关停/框架配置/多标签竞态 | W1(A2)、W10(J6)、W4(D2-7)、W8、W3(C4-7) |
| P12 跨切面治理 | W11(L1-L3) |
| P13 开发者体验 | W10(K3) |
| P14 终验闭环 | W14 |

### 不兼容变更

- 触发命令不变（`/full-repo-audit`），但**行为变了**：不再直接进入 UI 审查，
  先跑 Phase 0 建档；`commit/push/PR` 默认需要确认。
- v1 的 Phase 编号（P1-P14）不再使用，改为 W1-W14 + 维度 ID（A1-L4）。
  发现台账与报告引用维度 ID，不引用 Phase 编号。

---

## v1.0.0 — 2026-09

从一次真实审查战役（14 轮、68+ agent、~900 文件、200+ 问题）提炼的首版 skill。
